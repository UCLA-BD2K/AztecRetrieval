extends layout

block content
    h1= title
    p Bookshelf Status:
        if bookshelfStatus == "READY"
            #connReady READY
        else
            #connErr NOT READY
    p Mongo Status:
        if mongoStatus == "READY"
            #connReady READY
        else
            #connErr NOT READY
    p Solr Status:
        if solrStatus == "READY"
            #connReady READY
        else
            #connErr NOT READY
    hr
    p Available actions:
    ul#actions
        li
            a(href="./update") Retrieve and update ALL
        li
            a(href="./biocatalog/retrieveAndUpdate") Retrieve and update Biocatalog
        li
            a(href="./bioconductor/retrieveAndUpdate") Retrieve and update Bio
        li
            a(href="./biojs/retrieveAndUpdate") Retrieve and update BioJS
        li
            a(href="./cytoscape/retrieveAndUpdate") Retrieve and update Cytoscape
        li
            a(href="./sourceforge/retrieveAndUpdate") Retrieve and update Sourceforge
